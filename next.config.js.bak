const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "http",
        hostname: "cms.pentas.tv",
      },
      {
        protocol: "https",
        hostname: "cms.pentas.tv",
      },
    ],
  },

  async redirects() {
    return [
      {
        source: "/:category/:slug",
        destination: "/berita/:slug",
        permanent: true,
      },
    ];
  },
};

module.exports = nextConfig;